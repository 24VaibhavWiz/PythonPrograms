get_last _digit=lambda no:no%10
add=lambda x,y:x+y
del_last_degit=lambda no:no/10
def sum(no):
	sum=0
	while no>0
		last = get_last_digit(no)
		sum=add
		no=del_last_degit(no)
	return sum
def main():
		no=input"Please enter the number of your choice"
		print sum(no)
main()